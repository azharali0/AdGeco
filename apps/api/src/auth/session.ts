import { SignJWT, jwtVerify } from 'jose';
import type { TenantRole } from '@adgeco/contracts';
export interface SessionClaims { sub:string; organisationId:string; roles:TenantRole[]; sessionId:string; type:'access'|'mfa'; }
const key=(secret:string)=>new TextEncoder().encode(secret);
export async function signAccessSession(claims:Omit<SessionClaims,'type'>, secret:string):Promise<string>{ return new SignJWT({organisationId:claims.organisationId,roles:claims.roles,sessionId:claims.sessionId,type:'access'}).setProtectedHeader({alg:'HS256'}).setSubject(claims.sub).setIssuedAt().setExpirationTime('15m').sign(key(secret)); }
export async function signMfaChallenge(claims:Omit<SessionClaims,'type'|'sessionId'>, secret:string):Promise<string>{ return new SignJWT({organisationId:claims.organisationId,roles:claims.roles,type:'mfa'}).setProtectedHeader({alg:'HS256'}).setSubject(claims.sub).setIssuedAt().setExpirationTime('5m').sign(key(secret)); }
export async function verifySession(token:string, secret:string, expected:'access'|'mfa'='access'):Promise<SessionClaims>{ const {payload}=await jwtVerify(token,key(secret)); if(!payload.sub || typeof payload.organisationId!=='string' || !Array.isArray(payload.roles) || payload.type!==expected) throw new Error('INVALID_SESSION'); return {sub:payload.sub,organisationId:payload.organisationId,roles:payload.roles as TenantRole[],sessionId:typeof payload.sessionId==='string'?payload.sessionId:'',type:expected}; }
