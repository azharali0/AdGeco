import http from 'node:http';import {openRtbToCanonical,runFirstPriceAuction,canonicalToOpenRtb} from './engine.mjs';
const port=Number(process.env.PORT||3010);const server=http.createServer(async(req,res)=>{if(req.url==='/health/live'){res.writeHead(200,{'content-type':'application/json'});return res.end(JSON.stringify({status:'ok',service:'exchange'}));}if(req.method==='POST'&&req.url==='/openrtb2/auction'){let raw='';for await(const chunk of req)raw+=chunk;try{const request=openRtbToCanonical(JSON.parse(raw));const candidates=[];const decision=runFirstPriceAuction(request,candidates);res.writeHead(200,{'content-type':'application/json'});res.end(JSON.stringify(canonicalToOpenRtb(decision)));}catch(e){res.writeHead(400,{'content-type':'application/json'});res.end(JSON.stringify({error:e.message}));}return;}res.writeHead(404);res.end();});server.listen(port,()=>console.log(JSON.stringify({service:'exchange',port})));
