export const templates={EMAIL_VERIFICATION:({name,url})=>({subject:'Verify your AdGeco account',text:`Hello ${name}, verify your account: ${url}`}),PASSWORD_RESET:({name,url})=>({subject:'Reset your AdGeco password',text:`Hello ${name}, reset your password: ${url}`}),PAYOUT_COMPLETED:({amount,currency,reference})=>({subject:'AdGeco payout completed',text:`Your payout of ${currency} ${amount} is complete. Reference: ${reference}`})};
export function renderTemplate(type,data){const template=templates[type];if(!template)throw new Error('UNKNOWN_NOTIFICATION_TEMPLATE');return template(data);}
