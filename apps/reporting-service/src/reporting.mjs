export function aggregateCampaign(rows){return rows.reduce((a,r)=>{a.requests+=r.requests||0;a.impressions+=r.impressions||0;a.clicks+=r.clicks||0;a.spendMinor+=r.spendMinor||0;a.revenueMinor+=r.revenueMinor||0;return a;},{requests:0,impressions:0,clicks:0,spendMinor:0,revenueMinor:0,get ctr(){return this.impressions?this.clicks/this.impressions:0},get fillRate(){return this.requests?this.impressions/this.requests:0}});}
export function csv(headers,rows){const esc=v=>`"${String(v??'').replaceAll('"','""')}"`;return [headers.map(esc).join(','),...rows.map(r=>headers.map(h=>esc(r[h])).join(','))].join('\n');}
