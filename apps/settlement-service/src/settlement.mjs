import {assert,retryDelayMs} from '../../../packages/runtime-core/src/index.mjs';
export function prepareSettlement({earnings,minimumPayoutMinor=5000,withholdingRateBps=0,currency='USD'}){assert(Array.isArray(earnings),'EARNINGS_REQUIRED');const grossMinor=earnings.filter(e=>e.status==='AVAILABLE'&&e.currency===currency).reduce((sum,e)=>sum+e.amountMinor,0);if(grossMinor<minimumPayoutMinor)return {eligible:false,grossMinor,reason:'BELOW_MINIMUM'};const withholdingMinor=Math.floor(grossMinor*withholdingRateBps/10_000);return {eligible:true,currency,grossMinor,withholdingMinor,netMinor:grossMinor-withholdingMinor,earningIds:earnings.filter(e=>e.status==='AVAILABLE'&&e.currency===currency).map(e=>e.id)};}
export function nextPayoutAttempt(attempt,errorCode){return {attempt:attempt+1,status:attempt>=4?'FAILED':'RETRY_SCHEDULED',retryAfterMs:attempt>=4?null:retryDelayMs(attempt+1,{baseMs:30_000,maxMs:86_400_000}),errorCode};}
