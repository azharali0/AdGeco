'use client';import Link from 'next/link';import {FormEvent,useState} from 'react';import {ShieldIcon} from '../../components/icons';const apiBase=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{await fetch(`${apiBase}/v1/auth/password/forgot`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMessage('If an account matches this email, a secure reset link has been sent.')}finally{setBusy(false)}}return <main className="status-page"><section className="status-card" style={{textAlign:'left'}}><div className="status-icon" style={{marginLeft:0}}><ShieldIcon className="icon"/></div><h1 style={{fontSize:36}}>Reset your password</h1><p className="muted">Enter your account email and we will send a one-time reset link.</p><form onSubmit={submit}><div className="field"><label htmlFor="email">Email address</label><input id="email" className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="button button-primary full" disabled={busy}>{busy?'Sending…':'Send reset link'}</button>{message&&<div className="form-message success">{message}</div>}</form><p className="small" style={{marginTop:20}}><Link href="/login">Return to sign in</Link></p></section></main>}
