'use client';import Link from 'next/link';import {FormEvent,useState} from 'react';import {saveSession} from '../../lib/api';import {ChartIcon,ShieldIcon} from '../../components/icons';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000'}/v1/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const body=await response.json();if(!response.ok){setError(String(body.code??'Sign in failed'));return}saveSession(body);window.location.href=body.organisation.type==='PUBLISHER'?'/publisher':body.organisation.type==='ADVERTISER'?'/advertiser-dashboard':body.organisation.type==='AGENCY'?'/agency':'/admin'}catch{setError('We could not reach the platform. Please try again.')}finally{setBusy(false)}}return <main className="auth-wrap"><section className="auth-copy"><div className="eyebrow"><span className="eyebrow-dot"/>Secure workspace</div><h1>Welcome back to your advertising operations.</h1><p className="lead">Access campaign, inventory, measurement, trust and financial workflows through one focused workspace.</p><div className="auth-art"><div className="grid grid-2"><div className="card"><ChartIcon className="icon"/><h3 style={{marginTop:14}}>Clear performance</h3><p className="small muted">Live operational views without visual clutter.</p></div><div className="card"><ShieldIcon className="icon"/><h3 style={{marginTop:14}}>Protected access</h3><p className="small muted">Organisation and role boundaries on every request.</p></div></div></div></section><section className="auth-form-wrap"><div className="auth-card"><h1>Sign in</h1><p className="muted">Enter your account details to continue.</p><form onSubmit={submit}><div className="field"><label htmlFor="email">Email address</label><input id="email" className="input" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label htmlFor="password">Password</label><input id="password" className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><div className="form-meta"><span className="muted">Secure organisation access</span><Link href="/forgot-password">Forgot password?</Link></div><button className="button button-primary full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button>{error&&<div className="form-message error" role="alert">{error}</div>}</form><p className="small muted" style={{marginTop:22}}>New to AdGeco? <Link href="/signup">Create an account</Link></p></div></section></main>}
