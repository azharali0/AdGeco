'use client';import Link from 'next/link';import {FormEvent,useState} from 'react';import {CheckIcon} from '../../components/icons';
const apiBase=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000';
export default function Signup(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[name,setName]=useState('');const[type,setType]=useState('PUBLISHER');const[message,setMessage]=useState('');const[ok,setOk]=useState(false);const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setMessage('');setBusy(true);try{const response=await fetch(`${apiBase}/v1/auth/register`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password,organisationName:name,organisationType:type})});const body=await response.json();setOk(response.ok);setMessage(response.ok?'Your account is ready for email verification. Check your inbox to continue.':String(body.code??'Registration failed.'))}catch{setOk(false);setMessage('We could not reach the platform. Please try again.')}finally{setBusy(false)}}return <main className="auth-wrap"><section className="auth-copy"><div className="eyebrow"><span className="eyebrow-dot"/>Create your workspace</div><h1>Start with the experience built for your organisation.</h1><p className="lead">Choose your role, verify your account and continue into a database-backed workspace.</p><div style={{display:'grid',gap:14,marginTop:26}}>{['Role-based dashboard and permissions','Clear onboarding and operational status','Secure sessions and verified organisation access'].map(x=><div key={x} style={{display:'flex',gap:12,alignItems:'center'}}><div className="icon-box" style={{width:34,height:34,margin:0}}><CheckIcon className="icon"/></div><strong>{x}</strong></div>)}</div></section><section className="auth-form-wrap"><div className="auth-card"><h1>Create account</h1><p className="muted">Set up your organisation workspace.</p><form onSubmit={submit}><div className="field"><label htmlFor="name">Organisation name</label><input id="name" className="input" value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label htmlFor="type">Organisation type</label><select id="type" className="select" value={type} onChange={e=>setType(e.target.value)}><option value="PUBLISHER">Publisher</option><option value="ADVERTISER">Advertiser</option><option value="AGENCY">Agency</option></select></div><div className="field"><label htmlFor="email">Work email</label><input id="email" className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/></div><div className="field"><label htmlFor="password">Password</label><input id="password" className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="new-password" minLength={12} required/><span className="small muted">Use at least 12 characters.</span></div><button className="button button-primary full" disabled={busy}>{busy?'Creating account…':'Create account'}</button>{message&&<div className={`form-message ${ok?'success':'error'}`} role="status">{message}</div>}</form><p className="small muted" style={{marginTop:22}}>Already registered? <Link href="/login">Sign in</Link></p></div></section></main>}
