import test from 'node:test';import assert from 'node:assert/strict';import {runFirstPriceAuction} from '../../apps/exchange-service/src/engine.mjs';
test('local deterministic auction sustains 50k decisions',()=>{const candidates=Array.from({length:20},(_,i)=>({id:`b${i}`,campaignId:`c${i}`,creativeId:`cr${i}`,advertiserId:`a${i}`,active:true,approved:true,budgetAvailableMinor:1_000_000,bidMinor:100+i,formats:['BANNER']}));const start=performance.now();for(let i=0;i<50_000;i++){const result=runFirstPriceAuction({requestId:`r${i}`,floorMinor:50,currency:'USD',format:'BANNER',publisherId:'p'},candidates);assert.equal(result.lineItemId,'b19');}const elapsed=performance.now()-start;assert.ok(elapsed<5000,`local benchmark exceeded 5s: ${elapsed}`);console.log(JSON.stringify({decisions:50000,elapsedMs:Math.round(elapsed),perSecond:Math.round(50000/(elapsed/1000))}));});
