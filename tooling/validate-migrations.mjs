import {readdir,readFile} from 'node:fs/promises'; import {join} from 'node:path'; import {fileURLToPath} from 'node:url';
const root=new URL('../packages/database/prisma/migrations/',import.meta.url);const rootPath=fileURLToPath(root);const dirs=(await readdir(rootPath,{withFileTypes:true})).filter(d=>d.isDirectory());if(dirs.length!==1)throw new Error(`Expected one consolidated baseline migration, found ${dirs.length}`);const sql=await readFile(join(rootPath,dirs[0].name,'migration.sql'),'utf8');if(!sql.includes('CREATE TABLE')||sql.includes('TODO'))throw new Error('Migration is not executable');console.log(`migration-ok:${dirs[0].name}`);
